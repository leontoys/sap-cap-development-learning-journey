{
  "name": "@capire/incidents",
  "version": "1.0.0",
  "dependencies": {
    "@sap/cds": ">=8",
    "express": "^4",
    "@cap-js/sqlite": ">=1"
  },
  "devDependencies": {
    "@cap-js/cds-test": "*",
    "@cap-js/audit-logging": ">=0.8.3",
    "@cap-js/change-tracking": "^1.0.6",
    "@cap-js/attachments": "^2"
  },
  "scripts": {
    "watch": "cds watch",
    "start": "node .",
    "jest": "npx jest --silent",
    "test": "chest",
        "prestart": "cds deploy --to sqlite:db.sqlite"
  },
  "sapux": [
    "app/incidents"
  ],
  "private": true
}
