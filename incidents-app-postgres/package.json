{
  "name": "@capire/incidents",
  "version": "1.0.0",
  "dependencies": {
    "@cap-js/postgres": "^2.0.5",
    "@sap/cds": ">=8",
    "express": "^4"
  },
  "devDependencies": {
    "@cap-js/attachments": "^2",
    "@cap-js/audit-logging": ">=0.8.3",
    "@cap-js/cds-test": "*",
    "@cap-js/change-tracking": "^1.0.6",
    "@cap-js/sqlite": ">=1"
  },
  "scripts": {
    "watch": "cds watch",
    "start": "cds-serve",
    "jest": "npx jest --silent",
    "test": "chest",
  "prestart": "cds deploy --to postgres",
    "deploy:db": "npx cds deploy --to \"$POOLER_URL\"",
  "start:no-deploy": "node ./srv/server.js"
  },
  "sapux": [
    "app/incidents"
  ],
  "private": true
}
