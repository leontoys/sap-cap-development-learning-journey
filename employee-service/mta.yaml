_schema-version: 3.3.0
ID: employee-service
version: 1.0.0
description: "A simple CAP project."
parameters:
  enable-parallel-deployments: true
  deploy_mode: html5-repo
build-parameters:
  before-all:
    - builder: custom
      commands:
        - npm ci
        - npx cds build --production
modules:
  - name: employee-service-srv
    type: nodejs
    path: gen/srv
    parameters:
      instances: 1
      buildpack: nodejs_buildpack
    build-parameters:
      builder: npm-ci
    provides:
      - name: srv-api # required by consumers of CAP services (e.g. approuter)
        properties:
          srv-url: ${default-url}
    requires:
      - name: employee-service-auth
      - name: employee-service-db
      - name: employee-service-destination

  - name: employee-service-db-deployer
    type: hdb
    path: gen/db
    parameters:
      buildpack: nodejs_buildpack
    requires:
      - name: employee-service-db

  - name: employee-service-app-deployer
    type: com.sap.application.content
    path: gen
    requires:
      - name: employee-service-auth
      - name: srv-api
      - name: employee-service-html5-repo-host
        parameters:
          content-target: true
          config:
            HTML5Runtime_enabled: true
    build-parameters:
      build-result: app/
      requires:
        - name: employeeserviceproject1
          artifacts:
            - project1.zip
          target-path: app/
    parameters:
      config:
        HTML5Runtime_enabled: true
        init_data:
          instance:
            existing_destinations_policy: update
            destinations:
              - Name: srv-api
                URL: ~{srv-api/srv-url}
                Authentication: NoAuthentication
                Type: HTTP
                ProxyType: Internet
                HTML5.ForwardAuthToken: true
                HTML5.DynamicDestination: true
              - Name: ui5
                URL: https://ui5.sap.com
                Authentication: NoAuthentication
                Type: HTTP
                ProxyType: Internet

  - name: employeeserviceproject1
    type: html5
    path: app/project1
    build-parameters:
      build-result: dist
      builder: custom
      commands:
        - npm ci
        - npm run build
      supported-platforms:
        []

  - name: employee-service-destinations
    type: com.sap.application.content
    requires:
      - name: employee-service-auth
        parameters:
          service-key:
            name: employee-service-auth-key
      - name: employee-service-html5-repo-host
        parameters:
          service-key:
            name: employee-service-html5-repo-host-key
      - name: srv-api
      - name: employee-service-destination
        parameters:
          content-target: true
    build-parameters:
      no-source: true
    parameters:
      content:
        instance:
          existing_destinations_policy: update
          destinations:
            - Name: employee-service-html5-repository
              ServiceInstanceName: employee-service-html5-repo-host
              ServiceKeyName: employee-service-html5-repo-host-key
              sap.cloud.service: employeeservice.service
            - Name: employee-service-auth
              Authentication: OAuth2UserTokenExchange
              ServiceInstanceName: employee-service-auth
              ServiceKeyName: employee-service-auth-key
              sap.cloud.service: employeeservice.service

resources:
  - name: employee-service-auth
    type: org.cloudfoundry.managed-service
    parameters:
      service: xsuaa
      service-plan: application
      path: ./xs-security.json
      config:
        xsappname: employee-service-${org}-${space}
        tenant-mode: dedicated
        role-collections:
          - name: 'employee (employee-service ${org}-${space})'
            description: 'generated'
            role-template-references:
              - '$XSAPPNAME.employee'
          - name: 'manager (employee-service ${org}-${space})'
            description: 'generated'
            role-template-references:
              - '$XSAPPNAME.manager'
  - name: employee-service-db
    type: com.sap.xs.hdi-container
    parameters:
      service: hana
      service-plan: hdi-shared
  - name: employee-service-destination
    type: org.cloudfoundry.managed-service
    parameters:
      service: destination
      service-plan: lite
      config:
        HTML5Runtime_enabled: true
        init_data:
          instance:
            existing_destinations_policy: update
            destinations:
              - Name: srv-api
                URL: ~{srv-api/srv-url}
                Authentication: NoAuthentication
                Type: HTTP
                ProxyType: Internet
                HTML5.ForwardAuthToken: true
                HTML5.DynamicDestination: true
    requires:
      - name: srv-api
        group: destinations
        properties:
          name: srv-api # must be used in xs-app.json as well
          url: ~{srv-url}
          forwardAuthToken: true

  - name: employee-service-html5-repo-host
    type: org.cloudfoundry.managed-service
    parameters:
      service: html5-apps-repo
      service-plan: app-host
  - name: employee-service-html5-runtime
    type: org.cloudfoundry.managed-service
    parameters:
      service: html5-apps-repo
      service-plan: app-runtime
